# Data Center ID
datacenter_id: 1

# Unique Worker ID
worker_id: 1

discovery:
  serviceName: snowizard
  isDisabled: true

server:

  applicationConnectors:
    - type: http
      port: 0
  adminConnectors:
    - type: http
      port: 0