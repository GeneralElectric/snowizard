# Data Center ID
datacenter_id: 1

# Unique Worker ID
worker_id: 1

# Swagger-specific options.
swagger:

  resourcePackage: com.ge.snowizard.application.resources

server:

  applicationConnectors:
    - type: http
      port: 0
  adminConnectors:
    - type: http
      port: 0